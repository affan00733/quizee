import React, { Component } from 'react';
import { ActivityIndicator, Image, Dimensions, ToastAndroid, View, ScrollView, SafeAreaView } from 'react-native'
import { Container, Header, DeckSwiper, Content, Card, CardItem, Thumbnail, Text, Button, Icon, Left, Body, Right, Item } from 'native-base';

export default class About extends Component{
    render(){
        return(
            <View>
                <Text>About</Text>
            </View>
        )
    }
}